package mapa;

import java.awt.image.BufferedImage;

// Criada somente para verificação posterior...
public class Empty extends Tile {

	public Empty(int x, int y, BufferedImage sprite) {
		super(x, y, sprite);
	}
}
