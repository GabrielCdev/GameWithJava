package mapa;

import java.awt.image.BufferedImage;

// Criada somente para verificacao posterior...
public class Solo extends Tile{

	public Solo(int x, int y, BufferedImage sprite) {
		super(x, y, sprite);
	}
}
